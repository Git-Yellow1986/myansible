- name: install nginx in local system
  hosts: local
  become: yes
  connections: local
  tasks:

  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present

  - name: start service
    ansibel.builtin.service:
      name: nginx
      state: started    